const router = require('express').Router()
const ScoreController = require('../controllers/Conjunction')

// router.get('/addScore', ScoreController.find)
// router.post('/addScore',ScoreController.findOne)

module.exports = router